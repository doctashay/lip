# Forks

Each directory under `forks/` represents one upstream OSS codebase maintained by Leopard Improvement Project.

Rules:

- keep each upstream isolated
- document provenance in `UPSTREAM.md`
- do not mix unrelated source drops into a shared catch-all tree
- record closed-source boundaries and integration assumptions clearly
